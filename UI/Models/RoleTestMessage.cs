namespace JwtAuthDemoUI.Models
{
    // Generic message response returned by role-based endpoints. Contains a simple message string that can be displayed in the UI.
    public class RoleTestMessage
    {
        public string Message { get; set; }
    }
}
